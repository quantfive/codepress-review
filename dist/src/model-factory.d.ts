import { ModelConfig } from "./types";
/**
 * Creates the appropriate AI model based on configuration.
 * Caches model instances to avoid recreating them.
 */
export declare function createModel(config: ModelConfig): Promise<any>;
//# sourceMappingURL=model-factory.d.ts.map